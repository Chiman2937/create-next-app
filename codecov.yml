codecov:
  branch: main

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0%
        if_not_found: success
    patch:
      default:
        target: auto
        threshold: 0%
        if_not_found: success