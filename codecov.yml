coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0%

comment:
  layout: 'header, diff, files, footer' # 모든 섹션 포함
  behavior: default
  require_changes: false
  require_base: true
  require_head: true
  hide_project_coverage: false
